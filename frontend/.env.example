# Set in Netlify: Site settings → Environment variables (then redeploy)
# Or export before local build: $env:VITE_API_URL="https://..."
# No trailing slash
VITE_API_URL=https://your-backend.onrender.com
